# Changelog

## v0.11.31 — 2026-08-03 (차도 양쪽도 다리 밑으로 — 차가 안개에서 나온다)

- **길 끝을 막던 건물 벽을 터널로 바꿨다** (요청). 골목 터널과 **같은 콘크리트·같은 문법**
  (갱구 액자 · 옹벽 · 걸레받이 · 천장 보 · 검은 안개) — 차가 지나므로 더 넓고(도로 폭 그대로)
  높다(4.6m). 재질은 골목 터널과 한 벌을 공유해 색이 갈라지지 않는다
  - 18m 밖에서 보이는 것이라 신축이음처럼 가까이서만 읽히는 디테일은 넣지 않았고,
    **광원도 두지 않았다** — 이 거리에서 기여가 없고 광원 수가 곧 성능이다 (visual-polish §4)
  - 안개 판은 **터널 단면에 맞췄다** (골목 터널과 반대). 골목은 판을 키워 벽에 파묻었지만
    여기는 감싸는 구조가 얇아 키우면 오히려 삐져나온다 — 실제로 갱구 위에 검은 띠가 생겼다
- **차가 검은 안개에서 나와 반대편 안개로 들어간다.** 예전 주행 범위(±15)는 터널 입구(±18)보다
  안쪽이라 **차가 허공에서 생겨났다** — 터널을 놓으면 그게 도드라진다.
  범위를 ±22로 늘리고 시간도 같은 비율로 늘려(2.6→3.8s) **속도는 그대로**(약 11.6m/s) 뒀다.
  치임 판정 창의 길이도 그대로다
- **`verify:balance`의 무결점 주행이 깨졌다가 하네스를 고쳐 되살렸다.**
  `passMain`이 **구간 시작**(도로 36m 앞)에서 초록을 기다린 뒤 걸어서, 도로에 닿는 시점의
  신호 위상이 우연에 맡겨져 있었다 — 차 주행 시간을 건드리자 그 우연이 뒤집혔다.
  **사람은 정지선에서 기다린다**: 하네스도 정지선까지 간 뒤 초록을 기다리게 했다.
  게임의 공정성 약속("초록에 출발하면 반드시 건넌다")은 그대로다
- 검증: 아침·밤 × 횡단보도 위에서 좌·우, 차가 나오는 순간 5컷.
  verify:sim 10건 · verify:balance 6케이스(걸음 69.0s) · verify:shots 통과

## v0.11.30 — 2026-08-03 (터널 속의 검은 안개 — 밖에서도 들여다보이지 않는다)

- **멀리서 볼 때 터널 안이 검게 막혀 보이게** 했다 (요청).
  장면 안개(FogExp2)는 **카메라 기준**이라 "저 안이 어둡다"를 만들지 못한다 —
  들어가야만 어두워졌다. 깊이를 막는 **검은 판 5장**을 터널 안에 세웠다
  (opacity 0.22→0.75, 겹쳐서 약 97% 차폐. 조명·안개를 타지 않아 밤이든 아침이든 똑같이 검다)
  - **첫 판을 전환 지점(4.5m)보다 살짝 앞(3.2·4.2m)에** 뒀다: 그 깊이의 어둠이 이미
    70~93%라 판을 지나는 순간이 눈에 띄지 않는다. **안개를 통과하면 다음 골목**이다
  - 판은 터널 단면보다 크게(14×11) — 단면에 맞추면 가까이서 테두리가 보여
    동심 사각형이 겹친 '검은 액자'로 읽혔다 (실측으로 걸렀다)
  - 뒤 터널도 같다 — 돌아보면 지나온 길이 검은 안개로 막혀 있다 ("돌아가는 길은 없다")
- **아침 터널 암전이 검정이 아니라 회색이었다** (이번에 드러난 기존 결함). 둘을 고쳤다:
  - **감마** — `Color`는 선형 값이라 `(1-d)`를 그냥 곱하면 화면(sRGB)에서는
    `(1-d)^(1/2.2)`만큼만 어두워진다. d=0.9에서 체감 밝기가 35%나 남았다 → `(1-d)^2.2`
  - **아침의 하늘색** — 밝은 색을 그대로 짙게 하면 검은 안개가 아니라 **흰 안개**가 낀다.
    다리 밑에는 하늘빛이 들지 않으므로 안개색을 밤 색으로 먼저 당긴다
  - 밤은 원래 안개색이 거의 검정이라 이 결함이 드러나지 않았다 — **아침 튜토리얼 전용 버그**
- 검증: 아침·밤 × PC·모바일로 접근(20m·6m)·갱구 앞·진입·통과 직후·빠져나옴·뒤돌아보기
  7컷씩. verify:sim 10건 · verify:balance 6케이스(걸음 68.8s) · verify:shots 통과

## v0.11.29 — 2026-08-03 (펀치라인을 터널 들어가기 전으로)

- **"…이건 못 참지."를 z −35.2 → −28.6으로** (요청). 터널 입구가 −36이라 예전 자리는
  0.8m 앞 — **자막이 뜨자마자 어둠 속으로 들어가 버렸다.** 이제 밝은 골목에서
  약 2.2초를 버틴 뒤 터널로 들어간다 (실측: −29부터 표시, −36.5에서 터널 진입)
- 자리는 **횡단보도를 다 건너자마자**(도로 −21.3~−27.7 밖). 도로 위에서는 띄우지 않는다 —
  신호와 차를 봐야 하는 구간이라 자막이 시선을 뺏으면 안 된다
- 결과적으로 튜토리얼 다섯 비트가 전부 "본 것을 본 자리에서" 말한다:
  정류장 → 20m 현수막이 보인다 → 12m 이름을 읽는다 → 건너고 나서 결심 → 먹자골목 입구

## v0.11.28 — 2026-08-03 (개업 현수막을 다리 밑 터널 위로)

- **현수막을 왼쪽 벽면 밀착에서 터널 위 난간으로 옮겼다** (요청).
  v0.11.27에서 자막 위치를 현수막에 맞췄다면, 이번엔 현수막을 고쳤다 —
  **벽과 평행한 물건은 다가갈수록 비스듬해져 안 읽힌다**는 게 문제의 뿌리였기 때문
  - 이제 **정면을 향한다**: 25m부터 읽히고 접근 내내 정면을 유지한다
    (모바일 실측 25m/17m/11m 전부 또렷. 예전에는 12m 한 지점에서만)
  - **골목의 소실점에 걸린다** — 눈이 이미 가 있는 자리이고, "저기로 가야 한다"가
    자막이 아니라 구도로 읽힌다. 고가·지하차도 난간의 개업 현수막은 이 동네의 실제 그림
  - ⚠ 터널은 공용 지오메트리지만 현수막은 **테마 4 소유** — 구간 4에서만 걸려 있다
- **높이는 난간(y5.25)**. 갱구 바로 위(y4.5)로 내려봤다가 되돌렸다 —
  ① 낮을수록 근접에서 프레임에 오래 남아 HUD를 더 가리고
  ② 갱구 상인방(y 3.4~4.0)과 y가 겹쳐 아랫줄 글자가 상했다
- 밤 확인: 어둠이 붉은색을 먹어 장식으로 남고, 빨간 신호등이 여전히 더 밝다 —
  **시그널 위계(신호 > 장식)가 지켜진다** (visual-polish §3 저채도 적은 이상 전용색)
- 남은 것: 터널 5m 앞에서는 현수막이 머리 위를 덮어 HUD와 겹친다.
  다리 밑을 지나는 물리적으로 옳은 그림이고 "…이건 못 참지" 비트와 겹쳐 두었다

## v0.11.27 — 2026-08-03 (퇴근길 튜토리얼 검토 — 대낮의 가로등·사라진 힌트·안 보이는 현수막)

첫 화면 다음 화면(마감 예산 2순위 — visual-polish §1 "처음 걷는 골목")을 헤드리스로
훑으면서 나온 결함 넷. **전부 튜토리얼에서만 나던 것**이라 밤 검증에는 안 잡혔다.

- **대낮인데 가로등이 켜져 있었다.** `setMorning`이 "아침엔 가로등 소등"으로 꺼놓은 것을
  바로 뒤 `applyAnomalies`가 되살렸다 — `lamp_flicker.reset`이 `lampBase ?? 26`으로
  되돌리는데, 아침에는 `applyDepth`가 일찍 반환해 `lampBase`가 아예 없기 때문.
  리셋에 아침 가드를 넣었다 (밤의 감광 사다리는 그대로 — 깊이 2 스크린샷으로 확인)
- **퇴근길 튜토리얼에서 조작 힌트가 아예 안 보였다.** 해제 조건이 절대 좌표(`z < -6`)인데
  튜토리얼은 `z=-8.64`에서 시작해 **첫 프레임에 이미 참** — 뜨자마자 사라졌다.
  가르쳐야 할 구간에서만 안 보이고 이미 배운 밤 귀갓길에서는 보이던 정확한 역전.
  **'띄운 지점에서 6m를 걸었는가'로 바꿨다** (절대 좌표로 재지 않는다).
  실측: PC·모바일 모두 시작 시 표시 → 3m 유지 → 6m 넘으면 해제
- **자막이 "FF-1204"라고 부르는데 화면에 현수막이 없었다** (모바일은 아예 프레임 밖).
  현수막은 왼쪽 벽면 밀착이라 **가까울수록 비스듬해져 오히려 안 읽힌다** — 모바일 실측:
  19m 붉은 천으로만 / **12m 글자 또렷** / 10m 왼쪽 잘림 / 4m 프레임 밖.
  옛 비트 위치(-28.2 = 현수막 2.8m 앞)는 최악의 자리였다.
  비트를 **-11(20m, "저 앞에 보인다") / -19(12m, 이름을 읽는다)**로 옮겼다 — 둘 다 횡단 전
- **자막이 흰 횡단보도 위에서 묻혔다**: 그림자 한 겹으로는 부족 → 세 겹으로

## v0.11.26 — 2026-08-03 (타이틀 카피 — 명분 한 줄·가벼운 버튼·물러난 영문 부제)

- **전제 문구를 "오늘따라 감자튀김이 먹고 싶었다. 그게 다였다."로** (요청).
  밖에 나온 명분만 남기고 가게 이름도 현수막도 말하지 않는다 — 퇴근길에 직접 읽는 것이
  튜토리얼의 첫 사건이기 때문(무설명 학습). "그게 다였다"는 회고체이고,
  **뒤에 무엇이 있었는지는 설명하지 않는다** (§6 공포를 설명하지 않는다)
- **첫 시작 버튼 "퇴근하기!"** (요청). visual-polish §6의 '느낌표 남발 금지'에 대한
  **의도된 단 하나의 예외**로 문서에 못박았다 — 게임 안에는 느낌표가 없고 여기만 쓴다.
  신장개업 튀김집·도장과 같은 유머 앵커이자, 가벼운 퇴근 기분과 밤의 낙차를 벌리는 장치
- **영문 부제를 더 어둡게** (요청): `.eng` opacity 0.55 → 0.28 + 자간 0.06em.
  한글 제목의 그림자 정도로만 남는다. `.sub`를 그대로 낮추면 이어하기 문구까지
  같이 어두워져 별도 클래스로 분리

## v0.11.25 — 2026-08-03 (타이틀 문구 다이어트 + 설정 항목 정렬)

- 전제 문구에서 가게 소개를 걷어냈다 (v0.11.26에서 명분 한 줄로 교체)
- **설정을 항목 단위로 나누고 전부 가운데 정렬**: `[조작 줄] + [설명 줄]`을 세로로 쌓고
  항목 사이는 아주 옅은 실선 하나. 레이블·조작·설명을 한 줄에 좌우로 늘어놓던 구조가
  화면 폭마다 들쭉날쭉했다. 이어폰 권장은 소리 항목에 합쳤다 (같은 이야기)
- 밝기 미리보기(peek)는 실드래그로 재확인 — 배경 투명, 밝기 항목만 남고 나머지 opacity 0

## v0.11.24 — 2026-08-03 (타이틀 한국어 줄바꿈이 깨지던 문제)

- 모바일 실측(390·360px)에서 두 곳이 깨져 있었다:
  인용문 "…새로 생긴 감자튀김집."이 **"감자튀김 / 집."으로 갈라지고**,
  설정 레이블 "밝기"가 **"밝 / 기"로 세로 쪼개졌다**
- `word-break: keep-all` (한국어 표준 — 기본값은 어절 한가운데도 끊는다) +
  익명 flex 아이템이던 레이블을 `span.lbl`로 감싸 `flex-shrink: 0`
- 밝기 슬라이더 고정폭 150px → `flex:1 1 130px, max 180px`

## v0.11.23 — 2026-08-03 (밤 = 스테이지를 데이터로 — stages.json)

게임 동작은 그대로다. **밤을 추가하는 데 코드가 필요 없게** 만드는 작업
(원칙: 콘텐츠는 데이터, 로직은 시스템 — 콘텐츠 추가에 코드 수정이 필요하면 설계 실패).

- **`src/data/stages.json` 신설** — 밤마다 달라지는 것만 담는다:
  `night` · `onboarding` · `intro` · `epilogue`. 접근자는 `data.ts`의
  `STAGES` · `STAGE_COUNT` · `stageOf(night)` (스키마: docs/stages.md §4)
- **`main.ts`의 밤 번호 하드코딩 10곳 → 3곳.** 남은 셋은 하드코딩이 아니다:
  이상현상 해금(`anomalies.json`이 값을 갖는다) · 도장 칸(`STAGE_COUNT` 파생) ·
  도장 카드 표시(`save.night > 1` = 스테이지 속성이 아니라 "진행이 있는가")
  - `night === 1` 다섯 곳(첫 구간 강제 정상 · 무이상 방지 · 퇴근길 튜토리얼 진입 ·
    조작 힌트 표시 · 힌트 해제) → **`stageOf(night).onboarding`** 하나로.
    이 다섯은 원래 한 가지("첫 밤의 학습 보장")였는데 조건이 다섯 군데 흩어져 있었다
  - `TEXT.intros`/`epilogues` 배열 + `Math.min` 클램프 → `stageOf(night)`.
    **밤 6+가 마지막 스테이지를 재사용하는 규칙은 시스템에 남겼다** (데이터가 아니다 —
    엔딩이 구현되면 사라질 임시 규칙이므로)
  - 도장 칸 수 하드코딩 5(다섯 곳) → `STAGE_COUNT` (= stages.json 길이)
- **미구현 시스템의 빈 필드는 넣지 않았다** (`setpiece`/`rules`/`ending`).
  아무도 읽지 않는 스키마는 "이미 연결돼 있다"는 거짓 신호를 준다 —
  세트피스 시스템이 생기는 커밋에서 필드도 함께 생긴다
- 실측(저장 시드로 **밤 1~9 전부** 기동): 밤 1 = 튜토리얼 진입, 밤 2~5 = 각자 인트로,
  밤 6+ = 밤 5 인트로 재사용, 도장 `○○○○○`→`●●●●●`(밤 6)→칸 밖 `●`(밤 7부터).
  이관 전과 동일. verify:sim 10건 · verify:balance 6케이스 · verify:shots 통과

## v0.11.22 — 2026-08-03 (뒤 터널 옆구리가 뚫려 있던 문제 — 앞뒤 완전 대칭)

- **돌아보면 터널 양옆으로 바깥이 보이던 것** 수정 (플레이테스트 보고: "양끝 부분이
  투명벽으로 보여서 외부가 보여"). 원인은 재질이 아니라 **없는 벽**이었다 —
  끝벽(개구부만 남기고 막는 벽)이 골목 앞쪽에만 있고 뒤에는 없어서, 골목 벽이 z=0에서
  그냥 끊기고 터널 옹벽(±2.4)과 골목 벽(±3.0) 사이가 빈 채로 남아 있었다
  - **끝벽을 골목 양 끝에 하나씩** 둔다 (뒤쪽 z=+0.5, 앞쪽 z=-L-0.5의 거울상).
    터널이 대칭이면 그것을 감싸는 벽도 대칭이어야 한다
- **앞뒤 터널이 이제 완전한 거울상이다** (요청: "터널모양 대칭으로"). v0.11.21에서는
  뒤 갱구만 안쪽으로 물려 뒀는데(가리는 끝벽이 없어 통행 구역을 침범했기 때문),
  끝벽이 생기면서 그 이유가 사라졌다 — `buildTunnel()`의 `portalU` 매개변수를 없앴다.
  **두 터널의 차이는 마감벽 거리 하나뿐**(앞 4m: 더 이어지는 것처럼 / 뒤 0.6m: 막힌 끝)
- 판정도 대칭으로: 뒤 끝벽 통과 방지 거리를 앞과 같은 0.5m로 맞추고, 구간 시작 위치를
  z=0 → **-0.5**로 옮겼다 (뒤 갱구 바로 앞 = 터널에서 막 나온 자리).
  시작하자마자 옆으로 걸어도 밀려나지 않는다
- 실측: 같은 거리(갱구에서 6m)에서 앞/뒤를 정면·좌·우로 찍어 대조 — 세 각도 모두
  투과 없음, 두 그림이 사실상 동일. 무결점 걸음 시간 68.8/68.9s (2회, 시작점 이동으로
  −0.15s). verify:sim 10건 · verify:balance 6케이스 · verify:shots 테마 5종 통과

## v0.11.21 — 2026-08-03 (다리 밑 터널 마감 — 싼티 걷어내기)

- **터널이 통짜 상자 6개였다** (요청: "싼티 안 나게, 양쪽 벽이랑도 어울리게").
  텍스처를 붙이는 대신 **그림자가 걸릴 면**을 만들었다 — 싼티는 재질이 아니라 형태의 결여다
  (에셋 0 · 로우폴리 + 안개 + 포인트라이트 노선 유지, visual-polish §5)
  - **갱구(액자)**: 좌우 기둥 + 상인방 + 물끊기 처마. 종이처럼 뚫린 구멍이 두께를 갖는다
  - **옹벽**: 세로 신축이음 4줄 · 걸레받이(아랫단) · 물끊기 띠(윗단) — 판때기가 벽이 된다
  - **천장**: 상판 밑에 보 4개. 고개를 들면 다리 바닥이 있다
  - **노면**: 측구(배수 홈) + 가로 이음선 2줄
  - **다리 상부**: 가장자리 보 + 난간 벽 + **난간 기둥 열**. 골목에서 하늘을 배경으로
    이 실루엣이 걸리는 것이 "다리 밑"을 말해주는 결정타였다
  - **등**: 천장 등기구 2개(갓 + 물린 발광부). **광원은 여전히 하나** — 광원 수가 곧
    성능이자 스타일이다(§4). 멀어지는 등 두 개가 공짜로 깊이를 만든다
- **콘크리트를 골목 벽에서 가져왔다** (요청의 "어울리게"): 옹벽 = 골목 왼쪽 벽(0x20263a),
  갱구·다리 옆면 = 오른쪽 벽(0x232838), 노면 = 골목 바닥(0x181c28).
  터널이 따로 노는 물건이 아니라 이 동네가 이어진 것으로 읽힌다
- **앞뒤 터널을 한 함수(`buildTunnel`)로 만든다.** 전환은 터널 한가운데 암흑에서 일어나고
  들어간 깊이 그대로 반대편의 같은 깊이로 옮겨진다 — 두 터널의 형태가 어긋나면 그 순간
  이음매가 드러난다. 손으로 두 번 짓던 것을 구조로 못박았다
- **새 발광은 전부 어둠 곡선에 물렸다**: 등기구 발광부(emissive)까지 `setTunnelDark`가
  끈다. 빛만 끄면 등이 암흑 속에 뜬 점으로 남아 이음매를 드러낸다. 실측으로 확인 —
  전환 지점 스크린샷은 완전한 검정
- **카메라가 콘크리트에 박히던 것** 수정: 터널 안 x 한계가 1.65였는데 옹벽 안쪽면은 1.5였다
  (0.15m 침범). 한계를 1.25로 좁히고, 터널 구조물이 갱구(u=0)부터 시작하도록 맞췄다 —
  예전에는 옹벽이 골목 쪽으로 1m 나와 통행 구역을 침범했다. 뒤 갱구에도 앞과 같은
  통과 방지를 넣었다. 실측: 진입 후 우측 이동 한계 2.6 → **1.25**, 밀려남 없음
- **`verify:balance`가 완주 시간을 걸음/신호 대기로 나눠 찍는다.** v0.11.20에서 넣은
  초록 대기가 주기(9초) 안에서 매번 달라 합계가 4~9초씩 흔들렸다 —
  69.1초는 고정값이 아니라 운 좋은 한 표본이었다. 걸음 시간은 2회 연속 **69.0s**로
  터널 개조 전후가 같다 (템포 예산과 대조할 값은 이쪽)
- 검증: tsc·빌드, verify:sim 10건, verify:balance 6케이스, verify:shots 테마 5종.
  터널 전용 스크래치 스크립트로 접근·진입·심부·전환 직후·뒤돌아보기 10컷 전/후 대조

## v0.11.20 — 2026-08-03 (world.ts 분해 + 검증이 삼키던 실패)

**게임 동작은 그대로다.** M3(밤 2~5·세트피스·이상현상 30종+) 착수 전 구조 정리와,
그 과정에서 실측으로 드러난 검증 스크립트 결함 수정.

- **`src/world.ts`(1114줄) → `src/world/` 9개 파일로 분해** (development.md 백로그 1건 해소).
  `main.ts`의 `from './world'`는 한 줄도 바뀌지 않는다 — 디렉터리 + index.ts 조립부
  - 경계는 **M3가 실제로 키우는 곳**에 그었다: 세트피스는 `themes/theme1..5.ts`,
    이상현상 30종+는 `effects.ts`로만 자란다. `layout.ts`(치수·신호 주기)·`kit.ts`(프리미티브·
    캔버스 텍스처)·`prefab.ts`(공용 복도·터널·차)·`runtime.ts`(매 프레임)는 M3에서 거의 안 자란다
  - **`hit` 맵의 타입 강제를 유지**했다: 각 테마가 자기 effect 키로 정확히 타이핑된 조각을
    내고, index.ts가 `Record<AnomalyEffect, …>`로 스프레드한다 → data.ts에 effect를 추가하면
    "어느 테마에 짚을 사물이 있는가"에 답할 때까지 컴파일이 안 된다
  - 검증: tsc·빌드 통과, verify:sim 10건, verify:balance 6케이스 (무결점 귀갓길 **69.1s** —
    분해 전과 동일). **stash로 기준선을 다시 찍어 스크린샷 전/후 대조** (같은 장면 확인)
- **`verify:shots`가 테마 3·2·1을 촬영하지 않고 있던 것** 수정 (이번에 실측으로 발견).
  테마 순회의 관찰 지점(z −20)이 정지선(−21.1) 코앞이라, 스크린샷 대기 0.7초 뒤
  곧장 횡단에 들어가 **점멸 끝물에 차에 치였다** → 접힘 → 같은 테마 재촬영 ×4 →
  깊이 한계로 `transition`에 갇힘. 세 테마의 스크린샷은 옛 파일 그대로였다
  - `passMain`이 차도(테마 4) 앞에서 **막 초록으로 바뀐 순간을 기다린다** (`waitForFreshGreen`).
    관찰 지점 −20은 그대로 뒀다 — 프레이밍은 결함이 아니었다
  - `passMain`이 **접힘을 삼키지 않는다**: `phase` 변화만 보면 치임·붙잡힘도 통과로 읽힌다.
    이제 통과 중 folds가 늘면 경고를 찍는다 (커버리지가 조용히 줄지 않게)
  - 실측: 테마 5→4→3→2→1 전부 촬영 확인 (구간 4 = 횡단보도·개업 현수막·빨간불)

## v0.11.19 — 2026-08-03 (차가 다음 구간까지 따라오던 문제)

- **정류장에서 출발한 차가 먹자골목까지 따라오던 버그** 수정 (플레이테스트 보고).
  차는 테마 그룹이 아니라 공용 그룹에 있어서, 구간이 바뀌어도 주행 애니메이션이 이어졌다
- 두 겹으로 막았다: ① 구간 전환 시 `stopCar()`로 즉시 거둔다
  ② 매 프레임 안전망 — 정류장 테마가 보이지 않으면 그 자리에서 주행을 끝낸다
- 실측: 차 주행 중(carX 15)에 터널을 지나 다음 구간으로 → 전환 직후·0.6초 뒤 모두 `carX null`

## v0.11.18 — 2026-08-03 (돌아가려 하면 골목이 돌려세운다)

- **보이지 않는 벽(v0.11.17 래칫)을 연출로 교체** (요청). 뒤 터널 깊숙이(0.62T) 들어가면
  걸음이 멈추고, 0.9초 뒤 **다시 골목 쪽을 보고 있다**:
  "…돌아가려던 참이었는데. / 어느새 다시, 이쪽을 보고 있다."
- 왜 이쪽이 맞는가: 벽은 "게임이 막았다"는 신호지만 돌려세우기는 **"골목이 안 놓아준다"**는
  사건이다. 지나치면 길이 늘어나고(접힘) 돌아가려 하면 방향이 뒤집힌다 — 같은 문법의 두 얼굴
- 전환이 보이지 않는 것은 공짜로 얻었다: 그 지점은 이미 안개가 완전히 검은 곳이라
  방향을 돌리고 위치를 옮겨도 화면상 변화가 없다
- 발동 지점(0.62T)은 전환 직후 나오는 지점(0.5T)보다 깊게 잡았다 — 터널을 통과한 직후
  즉시 재발동하지 않도록. 계속 뒤로 가면 계속 돌려세워진다 (의도)
- 실측: 뒤로 걷기 → 암전 → 자막 → 앞으로 걸어 골목 복귀(z −4.55)

## v0.11.17 — 2026-08-03 (터널은 한 방향 — 암흑 막다른 끝 제거)

- **뒤 터널로 되돌아가면 캄캄해지고 아무것도 없는 막다른 끝에 갇히던 문제** 수정
  (플레이테스트 보고). 뒤 한계를 고정값(터널 끝)에서 **한 방향 래칫**으로 바꿨다 —
  앞으로 간 만큼만 줄어들고 **절대 늘어나지 않는다**. 터널에서 나오면 다시 들어갈 수 없다
- 왜 래칫인가: 이음매의 무봉합은 "터널 한가운데(4.5m)에서 어둠 = 1"에 기대는데,
  같은 지점을 되돌아 걸을 수 있으면 그 암흑을 **출구 없이** 만나게 된다.
  드나듦을 한 방향으로 잠그면 암흑은 지나가는 순간에만 존재한다
- 실측: 시작점 뒤로 0.5m(골목 입구까지만) / 8m 전진 후 뒤로 6.3m(온 만큼만) /
  **터널에서 나오자마자 뒤로 0.00m**(한 발도 못 간다) / 골목으로 나온 뒤 다시 뒤로 5.9m

## v0.11.16 — 2026-08-03 (다리 밑 터널 — 순간이동 대신 지나가는 공간)

- **구간 전환이 순간이동이었던 것**을 실제 공간으로 바꿨다 (요청).
  개구부를 지나면 **다리 밑 터널**(길이 9m, 폭은 개구부만큼, 천장 3.4m)로 들어가고,
  그 한가운데에서 다음 구간으로 넘어간다. 마지막 구간(도착)에서는 터널을 숨긴다 —
  그때는 터널이 아니라 가게·집이 나와야 한다
- **화면 페이드가 아니라 검은 안개로** (요청): DOM 오버레이 페이드를 걷어내고
  **안개 밀도·안개색·배경색·터널 등·앰비언트를 위치에 따라 함께 검정으로** 몬다.
  UI가 덮는 게 아니라 세계가 어두워지므로 '다리 밑을 지나는 동안'으로 읽힌다
  (옹벽이 1.5m 거리라 density 1.4까지 올려야 실제로 캄캄해진다 — 실측으로 잡은 값)
- **앞뒤 터널이 대칭이라 걸음이 끊기지 않는다**: 앞 터널로 절반(4.5m) 들어가면 그 지점이
  암흑이고, 거기서 **뒤 터널의 같은 깊이**로 옮겨진다. 두 지점의 어둠 값이 같아(0.99↔1.00)
  시각적 도약이 없고, 계속 걸으면 어둠이 풀리며 다음 골목으로 나온다
- **뒤에 있던 '빌라 현관'을 폐기하고 언제나 지나온 터널로** (플레이테스트 보고:
  "튜토리얼 4/5에서 뒤돌아보니 원룸이 보였다"). 퇴근길 출발점에도, 밤의 가게 앞 출발점에도
  내 집 현관은 앞뒤가 맞지 않았다 — 이 동네의 골목들은 터널로 이어져 있다는 설정으로 통일.
  뒤 터널 끝은 막혀 있다: **돌아가는 길은 없다** (접히는 골목의 정서와 맞물린다)
- 템포: 구간마다 9m가 늘어 **무결점 귀갓길 57.2s → 69.1s** (구간당 약 14초.
  템포 예산 1~2분 안. 길면 `TUNNEL_LEN` 상수 하나로 줄인다)
- 검증: tsc, verify:sim 10건, verify:balance 6케이스 전부 통과.
  연속 주행으로 골목 → 앞터널 → 암흑 전환 → 뒤터널 → 다음 골목이 끊김 없이 이어지는 것을
  프레임 단위 확인. 세 출발점(퇴근길·밤·터널 통과 후) 뒤돌아보기 육안

## v0.11.12 — 2026-08-03 (치임 문구 — 사고가 아니라 꿈으로)

- **차에 치였을 때 문구를 짧게, 꿈처럼** 바꿨다 (요청). 길게 설명하면 사고가 되고,
  짧게 흐리면 꿈이 된다 — 담담한 화자의 톤(design-principles §6)에도 이쪽이 맞다
  - 밤: "차에 치였다. 아니, 치인 것 같았다. / 눈을 떠 보니 아무도 없다. 차도, 자국도." (2줄)
    → **"차에 치인 것 같은 꿈을 꾼 것 같다."** (1줄).
    '같은…같다'의 겹침이 그대로 몽롱함이 된다. 뒤에 접힘 자각 문구가 붙어 두 줄로 완성
  - 낮(튜토리얼): 3줄 → **"차에 치인 것 같은 꿈을 꿨다. / …빨간불이었는데."** (2줄).
    낮은 꿈을 '꿨다'고 단정하고, 밤은 꿨는지조차 '같다'로 흐린다 — 확신의 차이로 낮과 밤을 가른다
- 검증: 낮·밤 각각 실제로 치여서 출력 문구 확인, 스크린샷 육안

## v0.11.13 — 2026-08-03 (현수막을 벽면으로, 그리고 "이건 못 참지")

- **현수막을 골목 가로지르기 → 왼쪽 벽면 밀착**으로 (요청). 매단 줄도 제거 —
  이제 통행 위를 덮지 않는다. 벽과 평행이라 멀리서는 비스듬히, 다가갈수록 펴져 읽힌다
  (실제로 골목을 걸으며 옆 간판을 읽는 각도). 자막 발화 지점은 요청대로 그대로 뒀고,
  두 지점(−16 "현수막이 보인다" / −28.2 "…감자튀김이다") 모두 의도대로 읽히는 것을 확인
- **자막 한 박자 추가** (요청): 먹자골목으로 들어서기 직전(z −35.2)에 **"…이건 못 참지."**
  앞 자막에서 2.2초 뒤 떨어지는 펀치라인이라, 힘든 하루 → 현수막 → 감자튀김 →
  **못 참는다**로 식욕이 한 번에 꺾여 들어간다 (하찮음의 유머 — design-principles §6)
- 검증: 튜토리얼 완주 시 자막 5개가 각각 −8.64 / −16 / −28.3 / −35.2 / T5 −12 에서 발화

## v0.11.11 — 2026-08-03 (퇴근길 자막을 걸음에 맞춰 — 본 것을 본 자리에서)

- **튜토리얼 자막을 한꺼번에 쏟지 않고 위치마다 하나씩** 터지게 바꿨다 (요청).
  세 문장을 시작하자마자 몰아주던 것을 걸음이 끌고 가도록 분해:
  | 지점 | 자막 |
  |---|---|
  | 정류장 앞 (스폰) | "힘든 하루였다." |
  | 신호등 앞 (z −16) | "…저 앞에 현수막이 보인다." |
  | 횡단보도를 막 벗어나며 (z −28.2) | "FF-1204. / …XXXXL 감자튀김이다." |
  | 먹자골목 입구 (5/5, z −12) | "먹자골목 입구. / 간판에 불이 켜져 있다." |
- **말과 화면이 맞물린다**: 두 번째 자막은 현수막이 저 앞에 처음 눈에 들어오는 자리,
  세 번째는 그 앞 4m에서 글자가 읽히는 자리. 처음엔 z −29로 잡았다가 현수막이 화면을
  덮어서 −28.2(도로 끝 −27.7 직후)로 당겼다 — 읽히되 덮지 않는 거리
- 구현: `TEXT.tutBeats`(theme·z·text 배열) + updateWalk의 진행 포인터.
  접힘으로 같은 구간을 다시 걸어도 이미 나온 자막은 다시 나오지 않는다
- 검증: 신호를 지키며 튜토리얼 완주 → **자막 4개가 각각 의도한 z에서 발화**
  (−8.64 / −16 / −28.44 / T5 −12), 스크린샷으로 말·화면 정합 확인

## v0.11.10 — 2026-08-03 (튜토리얼에서도 치이면 접힌다)

- **낮(튜토리얼)에도 차에 치이면 접힌다** (요청). 이전에는 경적만 울리고 지나갔다.
  튜토리얼이 안전지대인 것은 **괴담에 한해서**고, 차는 낮에도 차다 —
  규칙에 값이 있어야 배울 이유가 생긴다
  - 실측: "퇴근길 — 4/5" → 빨간불 강행 → 치임 → **"퇴근길 — 5/6"** (folds 1, depth 2, 구간 반복)
- 낮에는 **괴담이 새지 않게** 두 가지를 잠갔다:
  ① 접힘 자각 문구("…같은 골목이다. 걸어온 만큼, 늘어나 있다")를 붙이지 않는다
  ② 반복 구간 바닥의 분필 자국을 표시하지 않는다
  대신 낮 전용 문구로 자연스럽게 설명한다 — "…치였다. 눈을 떠 보니 정류장 벤치다.
  사람들이 지나간다. 빨간불이었다. 다음엔 기다리자."
- 정리: 쓰지 않게 된 경적 문구(`carHorn`)와 `onboard.horn` 플래그 제거

## v0.11.9 — 2026-08-03 (빨간불엔 차가 지나간다 — 세계의 규칙으로)

- **차를 '위반의 결과'에서 '세계의 규칙'으로** 바꿨다 (요청): 이제 **빨간불이 될 때마다
  차가 한 대 지나간다.** 내가 어겨서 불려오는 게 아니라 원래 그렇게 돈다
  - 덕분에 **서서 기다리는 동안 규칙을 배운다** — 신호가 빨강이 되고, 차가 지나가고,
    초록이 온다. 텍스트 한 줄 없이 인과가 보인다 (무설명 학습)
  - 길 위에 있으면 **치임 → 접힘**(암전 → 같은 구간 반복 + 깊이 +2). 판정은 그대로
  - 방향은 주기마다 좌↔우 번갈아 — 매번 같은 쪽에서 오지 않는다
- **차 소리 추가** (`audio.carPass`): 다가왔다 멀어지는 저역 럼블. 저역 통과 필터가 열렸다
  닫히며 거리감을 만든다. 화면을 안 보고 있어도 차가 오는 걸 안다 (fear-cognition §8
  시각 단서의 청각 병행). 기존의 `duck(정적)`은 차와 어울리지 않아 제거
- 검증: tsc, **초록 출발 → folds 0(무사) / 빨강 출발 → folds 1·depth 2(치임)** 재확인,
  정지선 앞 21초 대기 → **차 2회 통과·folds 0** (기다리면 안전하고, 규칙은 관찰된다)

## v0.11.8 — 2026-08-03 (신호등 배치 현실화 — 건널목 양 끝에 하나씩)

- **보행신호등 2기가 둘 다 횡단보도 앞에 나란히 서서 같은 쪽을 보던 것**을 교정 (요청).
  이제 건널목 **양 끝에 하나씩**, 서로 반대를 본다:
  - 건너편(길 건너 인도, x +) — 등화가 플레이어를 향한다. **이쪽에서 건너는 사람이 읽는 신호**
  - 이쪽(정지선 앞, x −) — 등화가 반대 방향. 우리에겐 **뒤통수만 보인다**
- 부수 효과로 판정 읽기가 더 자연스러워졌다: 신호를 확인하려면 길 건너를 봐야 한다
- 접근성 확인: 적색은 위(y 3.7)·녹색은 아래(y 3.42)라 **색이 아니라 위치로도 구분**된다
  (design-principles §9 색+형태 이중 표현)
- 검증: tsc, verify:sim 10건, verify:balance 6케이스, 낮·밤 × 초록·빨강 스크린샷 육안

## v0.11.7 — 2026-08-03 (신호등과 차 — 기다림이 처음으로 값을 갖는다)

- **신호를 어기면 차가 지나간다** (요청). 규칙은 하나: **빨간불에 길 위에 있으면 차가 온다.**
  치이면 죽지 않고 **접힘**(암전 → 같은 구간 반복 + 깊이 +2) — 이 게임의 다른 모든 실패와 같은 결과
  - 설계 의도: 기다리는 데 드는 시간이 이 게임 최초의 **자원**이다. 그리고 서 있는 동안은
    사람 형체(avert)에 취약해지므로, **통행과 관찰이 처음으로 충돌한다**
- **교차로를 실제로 뚫었다**: 지금까지 횡단보도가 양쪽이 막힌 골목 한가운데 그려져 있었다
  (기존 지오메트리 모순). 공용 벽을 도로 구간에서 갈라 두고 테마 1·2·3·5는 구멍을 메우는
  패치를 붙여, **테마 4만 열린 교차로**가 되게 했다. 건너편 노면·벽·길 끝 건물까지 채워
  구멍 너머로 허공이 보이지 않는다
- **점멸 신호로 공정성 확보** (실측 2회 교정): 최초 구현(초록 3.2s / 빨강 2.4s)에서는
  **초록에 출발해도 치였다** — 횡단 2.1초 동안 신호가 바뀌기 때문. 실제 횡단보도 문법을 도입해
  초록 3.5s → **점멸 2.5s** → 빨강 3.0s로 바꿨다. **점멸 구간이 횡단 시간보다 길므로
  초록에 출발하면 반드시 건넌다.** 점멸에 출발하는 것은 플레이어의 판단 — 서두르지 않으면 늦는다
  - 검증: 초록 출발 → folds 0 (무사 통과) / 빨강 출발 → folds 1, depth 2 (치임)
- **낮에는 배우고 밤에는 당한다**: 튜토리얼(구간 4를 지난다)에서 무단횡단하면 **경적만** 울린다
  ("빨간불이었다. 다음엔 기다리자") — 텍스트 설명 없이 규칙을 배우는 자리.
  밤에 같은 짓을 하면 치인다. 자막 "…빛이 온다. 이 시간에, 차가?"는 밤에만
- 정물성 원칙과의 관계: 차는 쫓아오지 않는다. 규칙 위반에 대한 **1회성 환경 반응**이므로
  "추격전 없음"과 충돌하지 않는다 (design-principles §1)
- 검증: tsc, verify:sim 10건, verify:balance 6케이스, **가림 검사 9종 × 8지점 전부 clear**
  (벽 구조를 바꿨으므로 회귀 확인), 차 통과 순간 스크린샷 육안

## v0.11.6 — 2026-08-02 (튜토리얼 스폰·현수막 재배치, 무료 쿠폰 약속 폐지)

- **튜토리얼 시작 위치를 정류장 앞으로** (요청): 구간 4의 입구(z=0)가 아니라 부스 곁
  (z=-L*0.24)에서 시작한다. 걸어온 게 아니라 **정류장에 서 있다가 걸음을 돌리는** 그림 —
  부스 지붕·벤치가 오른쪽에 들어온다. 튜토리얼 23.4s → **20.8s**
- **현수막을 구간 끝(먹자골목 입구 직전)으로 이동** (요청): z=-L*0.16 → **-L*0.9**.
  정류장에서 저 앞에 걸린 게 보이고, 걸어가서 그 아래를 지나 5/5로 들어간다.
  인트로 문구도 그에 맞게 교체 ("먹자골목 입구에 현수막이 걸려 있다 — … / 그냥 지나칠 수가 없었다")
- **"도장 5개 = 무료" 약속 폐지** (요청): 현수막에서 삭제(이제 'FF-1204 개업 / XXXXL 감자튀김'),
  타이틀 한 줄·쿠폰 카드 문구·meta description에서도 제거. 도장 카드는 **'방문 도장'**
  (진행 표시)으로 남는다 — 다섯 밤 구조 표시와 밤 6+ 칸 밖 도장 연출은 그대로
  - 딸린 문구 정리: 밤 5 에필로그("무료 쿠폰이 생겼다" → "도장을 다 채웠다"),
    튜토리얼 빈 지적("아침이라 그런가" → "아직 밝아서 그런가")
  - **엔딩 B "쿠폰을 쓴다"의 명분이 사라졌다** — game.md에 재설계 필요로 표시 (M3에서 확정)
- 검증: tsc, 튜토리얼 실주행(정류장 스폰 → 현수막 아래 통과 → 가게 → 밤 귀갓길) 스크린샷 육안

## v0.11.5 — 2026-08-02 (퇴근길 튜토리얼 — 5구간 → 2구간, 가게 이름 FF-1204)

- **튜토리얼을 퇴근길로 재구성** (요청): 아침에 집에서 출발하던 5구간 편도(57초)를 폐지하고,
  **정류장(4/5)에서 시작해 먹자골목 입구(5/5)까지 두 구간**만 걷는다 (**23.4초 실측**).
  인트로: "힘든 하루였다. / 정류장 현수막 — FF-1204, XXXXL 감자튀김. / …먹자골목으로 걸음을 돌렸다"
  - 동기가 자연스러워졌다: "도장 받으러 아침에 처음 가본다" → "퇴근길에 현수막 보고 들른다".
    시간 경과도 억지가 사라졌다 ("낮잠 자니 밤" → "정신없이 먹고 나오니 깜깜하다")
  - HUD 라벨 "첫날 아침" → **"퇴근길"**, 종료 컷 문구 교체
  - 대가(문서에 기록): 구간 1~3의 낮 모습을 못 보므로 밤에 처음 만난다 —
    '학습된 정상의 위반'은 구간 4·5에서만 성립하고 나머지는 '처음 보는 길'의 낯섦이 대신한다
- **개업 현수막 신규** (테마 4, 골목을 가로질러 y=3.5): 인트로 문구가 가리키는 실물.
  말한 것은 보여야 한다는 어포던스 원칙 — 시작하자마자 정면에 들어온다.
  밤에도 그대로 걸려 있지만 어둠에 먹혀 저채도로 보이므로 이상현상 신호색과 경쟁하지 않는다
  (가림 재검사: 정류장 형체 5지점 전부 clear)
- **가게 이름 감자상회 → FF-1204**: 간판 텍스처("FF-1204 24시" / 오탈자 "24시간요"),
  타이틀 한 줄, 쿠폰 카드, 도착 대사 전부 교체. 레포·URL(ff-1204.github.io)과 같은 이름이 되고,
  가게 이름이 제품 코드 같다는 위화감이 덤으로 붙는다. 사이즈 표기도 L → **XXXXL**
- **시작 버튼 문구 동적화**: 첫 방문 "퇴근길에 나선다" / 이어하기 "가게를 나선다"
  (시작 지점이 다른데 계속 "현관문을 연다"였던 오류 수정)
- 문서: story.md §2 전면 개정(+ 왜 퇴근길인가·대가 기록), spec.md·game.md·CLAUDE.md·README 갱신.
  **story.md §2가 v0.11.1 표지판 철거 때 조용히 누락**돼 있던 것도 함께 바로잡음
  (`String.replace`가 미스매치 시 조용히 통과한 탓 — 이후 배치 스크립트는 미적용을 보고하게 함)

## v0.11.4 — 2026-08-02 (가림 검사 도구 — 배치 3원칙 ②의 자동 검증)

- **`window.__fries.occlusion()`** 추가 (?a= 디버그 한정): 카메라에서 이상현상의
  **개별 메시**마다 레이캐스트해 시선이 가리는지 판정한다. 배치 3원칙 ②'관찰 시간 확보'를
  눈대중이 아니라 숫자로 확인하는 도구 — 프롭을 옮기거나 새 이상현상을 넣을 때 회귀 검사용
- `?anchor=0..5` — 그림자 사람의 스폰 앵커 고정 (기본 2). 6개 앵커 전수 검사에 필요
- 도구를 만들며 잡은 측정 함정 두 가지(주석으로 남김):
  ① `object.visible`만 보면 **숨긴 다른 테마의 프롭**이 차단물로 잡힌다 (5개 테마가 같은
  좌표를 공유) → 조상까지 확인하는 `shown()` 필요
  ② 그룹 **바운딩박스 중심**으로 쏘면 눈(구 2개)·핏자국(점점이)처럼 부품이 흩어진 대상은
  레이가 사이로 빠져나가 거짓 가림이 뜬다 → 메시 단위로 쏴야 한다
- **실측 결과: 가림 없음.** 이상현상 9종 × 구간 내 8지점, 그림자 사람 6앵커 × 테마 5개
  전부 `clear` — 통행 폭 안으로 들어온 프롭(배출장 1.0m·맥주 상자 0.8m)은 전부 낮고,
  나머지는 벽에 붙었거나 머리 위(어닝·돌출 간판)라 시선을 막지 않는다

## v0.11.3 — 2026-08-02 (느린 걸음 — 답답함이 공포다)

- **걷기 속도 4.2 → 3.2 m/s**. 달리기 폐지(v0.11.2)에 이어 기본 속도까지 낮춰,
  빨리 지나가는 것으로 공포를 건너뛸 수 없게 했다 (design-principles §4에 원칙 추가)
- **축별 속도 분리** — 앞으로 걷는 것 말고는 전부 굼뜨다:
  전진 3.2 / **좌우 1.6**(`strafeFactor 0.5`) / **뒷걸음 2.1**(`backFactor 0.65`).
  옆으로 피하거나 물러서는 몸짓이 답답해서, 통로에서 벗어나는 선택이 부담이 된다
- 발소리 간격 0.46 → 0.6초 (느린 보폭과 동기화)
- 실측: 전진 3.21 / 좌 1.60 / 뒷걸음 2.12 m/s (헤드리스 축별 실주행),
  무결점 귀갓길 **43.6s → 57.2s** (구간당 약 11초 — 템포 예산 1~2분 상한 안)
- 검증: tsc, verify:sim 10건(완주 시간 예산 테스트 포함), verify:balance 6케이스 전부 통과

## v0.11.2 — 2026-08-02 (달리기 폐지 — 속도는 하나뿐)

- **달리기 제거** (요청): PC Shift·모바일 "위로 밀기" 제스처 폐지. 이동 속도는 걷기 하나.
  담담한 걸음이 이 게임의 톤이고, "무서우면 뛴다"는 본능을 막는 것이 곧 긴장이다
- 정리 범위: `CONFIG.runSpeed` 삭제 · `input.getMove()` 반환에서 `run` 제거 ·
  `touchRun`·`.run` CSS 상태 제거(걷기 버튼은 홀드 하나) · 발소리 속도/음량 단일화 ·
  `balance.ts` 시뮬레이션의 `run` 옵션 제거 · verify.mjs `passMain(run)` 인자 제거 ·
  조작 안내 문구 3곳(온보딩 힌트·타이틀 ⚙ 설정 PC/터치·README)
- **달리기를 전제하던 기획 갱신**: 밤 3의 규칙형 괴담 "뛰면 안 되는 밤" →
  **"멈추면 안 되는 밤"**(game.md·story.md·anomalies.md) — 달리기 없이도 성립하고,
  귀갓길에서 발을 멈추게 만드는 avert 규칙과 오히려 더 팽팽하게 맞물린다
- 검증: tsc, verify:sim 10건, verify:balance 6케이스 전부 통과

## v0.11.1 — 2026-08-02 (표지판 철거 — 무설명 학습 원칙 복귀)

- **첫날 아침 튜토리얼 표지판 5종 제거** (요청). 아침 편도의 역할은 이제 하나뿐 —
  위협 없는 밝은 길에서 **정상 상태를 학습**하는 것. 규칙을 텍스트로 설명하지 않는다
  (design-principles §0: v0.10.0에서 열었던 "표지판 예외"를 닫고 무설명 원칙으로 복귀)
- 대신 **외면 규칙 온보딩 힌트 추가** — 표지판이 유일한 avert 학습 장치였으므로,
  기존 걷기·직시 힌트와 같은 패턴으로 첫 조우 1회만 (사람 형태 18m 접근 시 —
  응시 판정 거리 11m보다 먼저 떠서 붙잡히기 전에 알 수 있다). 온보딩 힌트는 이제 3종
- world.ts에서 boardTexture·tutBoard 지오메트리·setTutBoard 전량 제거 (SegmentRefs 축소)
- 문서 정리: design-principles §0·§1 표지판 예외 삭제 + 구 컨셉(약·약국·병) 잔재 일괄 교체,
  game.md·spec.md·story.md의 표지판 서술 갱신
- 검증: tsc, verify:sim 10건, **verify:balance 6케이스 전부 통과**, 아침 골목 스크린샷 육안

## v0.11.0 — 2026-08-02 (귀갓길이 게임 — 괴담의 존재들을 피해서 집으로)

- **구조 전환: 모든 밤이 귀갓길** (요청: "감자튀김 먹고 돌아가는 길에 괴담 요소").
  가는 길은 컷이고 튀김은 밖에 나온 명분으로만 쓴다 — 목적은 하나, **집에 도착하는 것**.
  v0.10.0의 밤 1 귀갓길을 게임 전체 구조로 승격(밤 편도 폐기), 테마는 5→1 역순.
  도착 등급을 귀가 서사로 재매핑(🏠 곧장 왔다 / 조금 돌았다 / 한참 걸렸다 —
  저장 스키마·balance.ts 그대로 재사용), 밤 사이는 "또 갔다 왔다" 컷으로 연결
- **판정 2축 — 직시와 외면** (신규 시스템. 데이터 스키마에 `rule` 추가):
  - **흔적**(OBJ, `gaze`) — 다가가서 직시해야 사라진다. 안 보고 나가면 접힘 (기존 유지)
  - **사람 형태**(HUM, `avert`) — **보면 붙잡힌다.** 시선축 12°·11m 안에 2.2초 담으면
    접힘과 같은 결과. 손가락질(클릭/탭)은 즉시. **지나치는 것이 정답**이므로 확인 의무 없음
  - 대응이 정반대인 두 부류가 같은 골목에 있어 **"저건 흔적인가 사람인가"의 판단**이
    생긴다 — v0.9.0에서 이상현상을 전부 명백하게 만들며 사라졌던 불확실성 축의 복원
  - 공정성 4요소: 거리·각도 제한 / 0.4초 경고 자막·정적·시야 조임 / 눈 떼면 2.5배속 회복 /
    통행선 옆을 스쳐 지나가는 것으로는 안 걸림 (design-principles §0에 명문화)
  - 임계 2회 실측 교정: 최초값(15m·NDC 0.3·1.7초)은 구간 진입 즉시 누적돼 반응 시간이
    없었고, NDC 기준은 앉은 형체처럼 낮은 대상에서 정조준해도 거짓 음성 → **시선축 각도**로 전환
- 첫날 아침 표지판 5종 중 하나를 avert 규칙 안내로 교체 — 아침에 두 규칙을 다 가르친다
- 디버그: `?avert=off` (응시 붙잡힘 정지 — 사람 형태 정면 촬영용).
  verify.mjs를 역순 구조에 맞춰 개편(구간 S 도달 = 5−S회 통과)하고 **E2E 6케이스로 확장** —
  손가락질 붙잡힘 / 외면 통과 무비용 신규
- 검증: tsc·verify:sim 10건, **verify:balance 6케이스 전부 통과**
  (무결점 귀갓길 43.6s → 🏠 곧장 왔다 → 밤 2 / 접힘 3회 → 리셋 / 직시 무비용 /
  빈 지적 깊이+1 / 손가락질 → folds 1·total 6 / 외면 통과 → folds 0),
  verify:shots 전량 + 응시 경고·붙잡힘 실주행 스크린샷 육안

## v0.10.0 — 2026-08-02 (컨셉 확정 — 감자튀김 회귀 + 첫날 아침 튜토리얼 + 밤 귀갓길)

- **세계관 회귀** (플레이테스트 피드백 — game.md 결정 기록): 약(FF-1204)·새벽약국(v0.9.0)
  → **신장개업 감자상회·도장 5개 = L 무료**(v0.8.1). 제목 「튀김이 식기 전에」 복원,
  타이틀·인게임 텍스트 전면 교체(도장 카드·시식·밤별 인트로/에필로그), 간판 '감자상회 24시'.
  v0.9.0 자산은 흡수 — **직시 소멸 판정**과 이상 오브젝트 9종(핏자국·백골·형체·눈)은 유지
- **첫날 아침 튜토리얼** (1일차 = 학습 스테이지의 확장): 밝은 아침 편도(setMorning —
  하늘·태양광·안개·가로등 전환), 이상현상 없음, 무엇을 짚어도 무비용.
  **골목 표지판 5종**(테마별)이 조작·규칙을 동네 안내판 톤으로 안내 — 아침엔 농담,
  밤엔 사실 (무설명 원칙의 예외를 첫날 아침 한정으로 명문화, design-principles §0 개정)
- **밤 1 귀갓길 = 본게임 시작**: 상회 도착·시식·첫 도장은 컷 → "나와 보니, 벌써 밤이다" →
  같은 구간을 테마 역순으로 되짚는다 (목적지 = 집, 간판 없이 개구부 불빛만).
  밤 2+는 기존 밤 편도 유지. softFail은 귀갓길이면 귀갓길로 재시작
- **구간 앞뒤 마감** — 돌아보거나 출구 틈으로 볼 때 하늘/허공이 보이던 구멍 제거.
  단순 가림판은 "상자 안"으로 보여서 **공간으로 읽히게** 재구성 (통행 판정 무관):
  뒤 = **내가 나온 빌라 현관**(파사드 + 1층 띠 + 문틀·현관·단 + 위층 창 4개, 스폰에서
  4.9m 뒤 · 골목 벽에서 이어지는 리턴 월) / 앞 = **T자 골목의 건너편 건물**(개구부 너머
  통로 4m로 깊이감 + 9.5m 뒤 건물 · 밝은 톤으로 거리감 · 1층 띠 · 창 2개, shopGlow가 비춘다).
  현관 안쪽은 완전 검정 대신 저명도(판정 대상 오인 방지 — §0 정직한 판정).
  **높이는 앞뒤 모두 옆벽과 동일(WALL_H)** — 골목은 위로 열려 있고, 하늘은 벽 위로 보인다
  (건너편 건물 위의 하늘 조각은 T자 골목의 자연스러운 화면으로 수용).
  아침·밤 × 180° 회전·개구부 근접 스크린샷 검증
- **수정: 게임 중 Space·Enter가 구간을 리셋하던 버그** — 숨긴 타이틀 버튼이 키보드
  포커스를 쥔 채 남아 있어 스페이스바가 시작 핸들러를 재실행했다(`pointer-events:none`은
  마우스만 막는다 — 걷던 중 z가 0으로 되돌아가고 history 항목도 쌓임).
  시작 시 포커스 해제 + 핸들러 재진입 가드(`if (started) return`)로 차단.
  실측: Space 입력 전후 z=-6.38 유지, focus=BODY 확인
- 저장 스키마: `tut`(튜토리얼 완료 — 중도 이탈 시 밤 1 귀갓길부터 재개, 구버전 저장은
  밤 2+면 자동 완료 처리). 디버그: `?a=`는 튜토리얼 생략(E2E 결정성 — verify.mjs 무수정
  통과), `?t=1`은 튜토리얼 강제(스크린샷 검증용)
- 문서 개편: game.md(컨셉·코어 루프·다섯 밤 표·엔딩), story.md(v0.8.1 복원 + 직시 소멸·
  첫날 아침 패치), spec.md 전면 갱신, design-principles §0·§1, anomalies.md 서두,
  CLAUDE.md·README·privacy.md 명칭 정리
- 검증: tsc·verify:sim 10건 통과, 헤드리스 실주행(아침 5구간 완주 28s → 상회 컷 →
  귀갓길 전환·테마 역순 5→4 실측) + 스크린샷 육안(아침 표지판·귀갓길·타이틀)

## v0.9.1 — 2026-08-02 (타이틀 다이어트 — 1일차 = 학습 스테이지)

- **타이틀 최소화** (플레이테스트 피드백: 첫 화면 정보 과다): 첫 방문 타이틀은
  시계·제목·전제 한 줄·시작 버튼·⚙ 설정만. 이어폰 권장·조작 참조는 설정 패널 안으로,
  **처방 카드는 재방문(진행 있음)에만** 표시 (진행 표시 역할이므로 첫 방문엔 정보가 아니라 소음)
- **1일차 온보딩 힌트** — 조작 학습을 타이틀에서 스테이지 안으로 이관:
  ① 걷기 힌트(시작 직후 상단, 6m 걸으면 사라짐 — 해보면 배운 것)
  ② 직시 힌트(첫 환각 12m 접근 시 한 번만, PC 클릭/터치 탭 문구 분기).
  규칙(접힘·깊이)은 계속 무설명 — reveal·즉각 인과가 담당 (design-principles §0)
- hud.showTouchHint → showHint/hideHint 일반화 (#touch-hint 요소 재사용)
- **개인정보처리방침 개정** (docs/privacy.md — 시행일 2026-08-02): 게임명 변경 반영
  (구 제목·URL 유래 설명 추가), 아동의 개인정보 항목 신설, 변경 이력 절 추가,
  저장 항목 문구 정비. 수집 항목·처리 방식 변경 없음. 타이틀 하단 링크를
  fineprint에서 분리해 중앙 정렬 고스트 버튼으로
- 검증: tsc 통과, 헤드리스 스크린샷 8종(PC/모바일 × 첫 방문/설정/재방문/인게임 힌트) 육안 +
  힌트 표시·해제·전환을 상태 실측(z 좌표·DOM)으로 확인

## v0.9.0 — 2026-08-02 (컨셉 전환 — 약(FF-1204)을 사러 가는 길)

- **전제 교체** (game.md 결정 기록, story.md 전면 개정): 감자튀김 → **약 FF-1204**.
  하루치씩만 주는 24시 새벽약국, 5일분 처방 = 다섯 밤. 약 기운이 떨어질수록 아파지고
  환각이 보인다. 게임명 「약효가 다하기 전에 (Before the Meds Wear Off)」 — 레포·URL 유지
- **환각 시스템** (요청: 일상 사물 어긋남 대신 귀신·해골·핏자국류):
  신규 환각 8종 구현 — 바닥 핏자국·배출장 백골·창문의 얼굴·셔터 손자국·그네의 형체·
  어둠 속 눈·정류장의 형체·끌린 자국 (+기존 그림자 사람 = 풀 9종).
  구 일상 어긋남 14종은 풀에서 퇴역 (핸들러·배경 사물은 잔존)
- **판정 전환: 직시 소멸** — 환각은 다가가서(4.5m) 똑바로 보면 사라진다
  ("원래, 없던 것이다"). 확인 잔존 문법 폐기 — 환각 서사의 즉각 보상
- **통증 비네트** — 깊이(병세)에 따라 화면 가장자리가 저채도 적으로 조여들고
  깊이 4+에서 느린 맥동 (가로등 감광의 신체 감각 보조)
- **프롤로그 제거** — 폰 시퀀스 삭제, 타이틀 단일 게이트 (01:04 · 처방 카드 ·
  "약이 떨어졌다" 한 줄). 쿠폰 카드 → 처방 카드(복약 체크, 6일차+ 칸 밖 체크)
- 인게임 텍스트 전면 교체 (일차 라벨 "N일차", 인트로/에필로그 10종 — 통증→환각→벗어남
  아치, 새벽약국 도착·복약 연출 💊, 간판 '새벽약국 24시')
- 검증: verify:sim 10건, E2E 4케이스(무결점 43.5s·접힘 리셋·직시 통과·빈 의심) 통과,
  환각 9종 + 테마 5종 헤드리스 스크린샷 육안 (verify.mjs 케이스 개편 — 프롤로그 제거 반영)

## v0.8.1 — 2026-08-02 (밝기 재조정 — 바닥을 올리고 분위기는 대비로)

- **전체 노출 배율 `EXPOSURE_BASE 1.35`** — 저장된 밝기 설정 의미(1.0 = 기본)를 유지한 채
  곱연산 상향: 기존 저장 사용자도 마이그레이션 없이 함께 밝아진다
- 조명 상향: 앰비언트 1.1→2.2(색 0x2a3050→0x39415e), 달빛 0.4→0.75,
  가로등 사다리 [22,16,11,7,4,2]→[26,20,14,9,5.5,3] (단차 유지 — 깊이 게이지 역할 보존),
  깊이별 앰비언트 바닥 0.55→1.1
- 안개 기본 밀도 0.052→0.044, 게임 화면 비네트 0.34→0.22
- 원칙: 공포는 어둠의 절대량이 아니라 **웜/한색 대비·안개·감광 사다리**가 담당
  (visual-polish §3·§4) — 깊이 4 감광의 어두움 체감은 스크린샷으로 유지 확인

## v0.8.0 — 2026-08-02 (스토리 개편 — 신장개업 튀김집·처음 가는 길·3막 목적 전도)

- **전제 교체** (game.md 결정 기록, story.md 전면 개정): 단골 버거집 → **처음 가보는
  골목 끝에 신장개업한 '감자상회'** (배달 불가·포장만·24시간·리뷰 0).
  개업 이벤트 **도장 5개 = L 무료**가 다섯 밤의 디제틱 명분
- **3막 아치**: 초반 낯섦(밤 1–2) → 중반 홀림·어긋남의 개인화(밤 3–4) → 후반 목적
  전도(밤 5 — 튀김이 아니라 귀가가 목표, 기존 귀갓길 세트피스·엔딩 A와 수렴)
- **쿠폰 도장 카드 UI**: 타이틀 화면 디제틱 진행 표시 (지난 밤 수 = 도장).
  밤 6+는 여섯 번째 도장이 칸 밖에 + "…여섯 번째 도장은, 칸 밖에 찍혀 있다."
  시식 화면에도 도장 진행 한 줄 (밤 6+: "…도장 찍을 칸이, 이제 없다")
- 인게임 텍스트 전면 교체: 프롤로그 폰(신규 입점·도장 이벤트·포장 전용), 타이틀 인용구
  ("앱에는 그렇게 적혀 있다"), 밤별 인트로·에필로그 10종(3막 아치), 감자상회 도착·구매 문구,
  간판 텍스처('감자상회 24시', A-012 오탈자 개그 유지), 이어하기 문구(침대→골목 입구 교정)
- 검증: verify:sim 10건 통과, 헤드리스 스크린샷(프롤로그·타이틀 도장 0/2/6개) 육안 확인

## v0.7.0 — 2026-08-02 (UI 컨셉 마감 + 모바일 조작 단순화)

- **모바일 조작 재설계 — 조이스틱·반분할 폐지**: 길이 직진뿐이라 조향이 필요 없다는
  판정 구조에 맞춰 3동작으로 축소 — ① **"걷는다" 버튼 홀드** = 전진 (하단 왼손,
  위로 밀면 "달린다"), ② **화면 어디든 드래그** = 시점, ③ **탭** = 확인. 손을 떼면
  그 자리에서 관찰 — 걷기와 관찰의 전환이 조작 그 자체가 된다
- 터치 기기에서 크로스헤어 숨김 — 지적은 탭 지점이므로 중앙 점은 오해를 만든다
  (`@media (pointer: coarse)`). 시작 화면 조작 안내도 PC/터치 분리(.pc-only/.touch-only)
- **컨셉 마감** (visual-polish 팔레트·이징 준수): 타이틀 앰버 글로우(간판의 온기) +
  01:04 시계 모티프(프롤로그 폰 상태바와 동일 시각 — 디제틱 앵커), 게임 화면 비네트
  (광원 안쪽 배치 원칙이라 판정 공정성과 무충돌), 구간 카운터 앰버 이정표 악센트,
  주 행동 버튼 숨쉬는 글로우(3.4s 명멸 — 팝·바운스 금지 준수)
- 조작 힌트 상단 이동 + 줄바꿈 허용 — 하단 자막(#msg)·걷기 버튼과 겹침 해소
- 검증: verify:sim 통과, 헤드리스 스크린샷 4종(PC/모바일 × 타이틀/인게임) 육안 확인,
  걷기 버튼 홀드 실주행 실측(1.6s에 z −7.3m ✓)

## v0.6.0 — 2026-08-02 (확인 의무·증식·스폰 포인트 — 공포 상향)

- **근접 확인**: 지적이 4.5m 이내에서만 성립 — 멀리서 짚으면 "…여기서는 잘 안 보인다.
  더 가까이 가야 한다" (비용 없음). 무서운 것에 다가가는 것이 공포 코어가 되도록
- **증식**: 확인 없이 지나칠 때마다 이후 구간의 동시 이상 수 +1 (1→2→3 상한).
  구간의 이상을 **전부** 확인해야 통과 — 지나칠수록 밤은 길고(접힘) 어둡고(깊이) 붐빈다(증식)
- **스폰 포인트 시스템**: `segment: 0` = 위치 자유형 — 스폰 앵커 6곳 중 랜덤 출현.
  신규 이상현상 **A-010 그림자 사람**(HUM, 밤 3 해금): 등을 돌리고 서 있는 실루엣,
  움직임·위협 없음 (15세 원칙 — 그냥 서 있는 것이 가장 무섭다)
- 다중 이상 지원: applyAnomalies(복수 effect 동시 적용), 이상별 확인 상태 추적,
  연속 등장 방지가 직전 구간의 전체 id 집합 기준으로 확장
- 검증: verify:sim 10건(증식 상한 추가), E2E 4케이스 통과(확인 통과·빈 지적 포함),
  figure 스크린샷(디버그 앵커 고정으로 결정성 확보) 육안 확인. 워딩: "짚기"→"확인"

## v0.5.0 — 2026-08-02 (판정 동사 전환 — 갈림길 폐기, 직진 + 지적)

- **본길/샛길 갈림길 폐기** — 왼쪽 벽 통짜, 출구는 구간 끝 개구부 하나. 이동은 직진뿐
  (사유: 갈림길 판정이 "계속 걷는다"는 장르의 몸짓과 충돌 — game.md 결정 기록)
- **지적 판정**: 이상하다고 생각하는 사물을 직접 짚는다 — PC: 크로스헤어(걷는 동안 표시) +
  클릭 / 모바일: 탭 (드래그와 자연 구분: 350ms·14px 이내). 레이캐스트 + 관용 반경 판정.
  짚어도 이상은 사라지지 않는다 — "…봤다. 골목도, 내가 봤다는 걸 안다"
- 비용 재매핑: 이상을 짚고 통과 = 무비용 / 못 보고 나가면 = 접힘(깊이 +2) /
  빈 지적 = 깊이 +1 + 즉각 가로등 감광. 연타는 쿨다운 0.6초
- effect별 히트 대상 맵(world.ts `hit`) — Record 타입이라 새 effect 추가 시 누락이 컴파일 에러
- 검증: verify:sim 9건(깊이 모델 재작성) + E2E 4케이스(무결점 43.5s→밤 2 / 접힘 3회→입구
  리셋 / 지적 통과 무비용 / 빈 지적 깊이 +1 — `debugSpot` 검증 훅은 ?a= 디버그 한정) +
  스크린샷 18장 육안 확인. 조작 안내에 "이상한 것은 클릭/탭으로 짚기" 한 줄 추가

## v0.4.0 — 2026-08-02 (M2 컨셉 전환 — 접히는 골목)

- **판정 결과 재정의** (game.md 2026-08-02 결정): 오답 본길 = 실패(암전)가 아니라 **접힘** —
  같은 테마 구간이 반복되고 총 구간 수 +1 (카운터 "3/5"→"4/6"), 깊이 +2.
  과잉 경계(정상인데 샛길) = 깊이 +1. 정당 우회는 무비용 (공정성)
- **깊이 시스템**: 가로등 감광 사다리(22→2, 6단계) + 앰비언트·안개 연동 = 다이제틱 게이지.
  깊이 6 도달 = 암전 → "정신을 차려 보니, 골목 입구다" (soft fail — 같은 밤 재시작, 죽음 묘사 없음)
- **접힘 인지 4요소 구현**: 카운터 페이드 교체(팝 금지) · reveal+자각 자막(줄바꿈 지원) ·
  사운드 드랍 · 반복 구간 바닥의 분필 원 표식
- **온도·왕복 퇴역**: temp 게이지·귀갓길 분기·시식 등급 판정 제거. 도착 = 밤 종료,
  귀가는 컷. 시식 연출은 유지하되 감자튀김 온도는 접힘 횟수를 비추는 서사로만
  (접힘 0 = 바삭 / 1~2 = 미지근 / 3+ = 눅눅 — 저장 스키마 호환). 노미스 추적 = 접힘 0
- 검증: verify:sim 9건(깊이 모델 — 접힘 3회=리셋, 과잉만 6회=리셋, 정당 우회 무비용 등),
  E2E 3케이스(무결점 밤 43.5s→밤 2 진입 / 접힘 3회→입구 리셋 / 정당 우회 무비용) 전부 통과,
  접힘 시각 요소 스크린샷 2종 추가 육안 확인

## v0.3.5 — 2026-08-02 (밸런스 순수 시뮬레이션 테스트 — 브라우저 없이 <1초)

- `npm run verify:sim` — 밸런스 3목표(걷기=미지근/질주=바삭/과잉우회2=눅눅)를 수식으로
  검증하는 node:test 테스트 7건. 실브라우저 E2E(≈5분)는 릴리즈 전 최종 확인으로 역할 분리
- 수치 상수를 `src/config.ts`로 분리 (JSON·three 의존 없음 — Node가 직접 import),
  온도·시식 판정 수식을 `src/balance.ts`로 추출 — main.ts와 테스트가 같은 소스를 읽어
  사본 드리프트 원천 차단. 기존 import 경로는 data.ts/world.ts 재수출로 유지
- 추가 검증: 정당 우회 2회는 미지근 유지(공정성), 등급 경계값, 비용 비대칭(×0.5/×1.5),
  실측 정합 ±3%p (시뮬레이션 61.0/76.5/24.4 vs E2E 61/77/24)
- E2E 재실측으로 리팩터 동작 불변 확인 (61%/77%/24% 동일). @types/node devDependency 추가
  (licenses.md 기록). 게임 로직 변화 없음

## v0.3.4 — 2026-08-02 (이상현상 핸들러 레지스트리 — M2/M3 대비 부채 정리)

- `applyAnomaly`의 수동 리셋 나열 + switch를 **effect별 핸들러 레지스트리**
  (`Record<AnomalyEffect, {reset, apply?}>`)로 재구성 — reset이 effect 정의에 붙어 있고
  타입이 전 effect 항목을 강제하므로, 새 effect 추가 시 리셋 누락(이상 상태가 다음
  구간으로 새는 함정)이 컴파일 에러로 막힌다. 동작 변화 없음 (1:1 이전)
- `window.__fries.config()` 훅 추가 (CONFIG + 샛길 개구부 좌표) — verify.mjs의 손계산
  상수 `SIDE_GAP_Z = -28.5` 제거, 개구부 중앙을 실좌표에서 파생 (구간 길이 변경에 자동 추종)
- 검증: verify:shots 16장 (TXT 3종 정상/이상 쌍 + 테마 5종) 육안 확인,
  verify:balance 3목표 통과 — 걷기 61% 미지근 / 질주 77% 바삭 / 걷기+과잉우회 2회 24% 눅눅
  (우회 진입점이 개구부 중앙으로 바뀌며 27%→24%, 목표 등급 불변)

## v0.3.3 — 2026-07-30 (맵 구조 수정 — 벽면 파사드 전환)

- 세탁소(구간 2)·정류장 부스(구간 4)가 통로에 **수직으로 꽂힌 M0 슬랩**이어서
  골목을 반쯤 막은 벽처럼 보이고(막다른 길 오인), 눈높이 높이라 우벽에 붙어 걸으면
  카메라가 관통하던 문제 — 둘 다 **벽과 평행한 파사드**로 재구성
  (세탁소는 왼쪽 셔터 가게들과 같은 문법, 부스는 등판이 벽을 등지고 기둥은 통행 한계 밖)
- 그네 프레임 x -2.5 → -2.78 — 통행 한계(-2.6) 밖으로 (기둥 몸통 통과 제거)
- 검증: 우벽 밀착 주행으로 관통 재현 → 수정 후 해소 확인, A-004(셔터 반열림)·A-007(그네)
  가시성 재확인 — 반열림 세탁소는 벽면 파사드에서 오히려 더 명확해짐

## v0.3.2 — 2026-07-30 (M1 마무리 — TXT 이상현상·구간 구조·시식 연출·밸런싱)

- TXT 계열 이상현상 3종 (밤 2 해금, 캔버스 텍스트 렌더 — 시스템 폰트, 웹폰트 없음):
  과외 전단지 전화번호 11→10자리(A-003), 부동산 시세표 "월세 35"→"급구"(A-005),
  버거집 간판 "버거버거 24시"→"24시간요"(A-012). 정상 상태 사물(전단지·시세표·간판 글자)은
  밤 1부터 상시 노출 — "학습된 정상"이 먼저 (fear-cognition §1)
- 스키마 확장: `outboundOnly` — 귀갓길에 보이지 않는 사물(버거집 간판)은 귀갓길 풀에서 제외 (공정성)
- 버거집 간판을 끝벽 앞으로 이동 (기둥에 좌우가 가려 오탈자가 잘리던 문제) +
  마지막 구간에서만 표시 (다른 구간 끝에서 글자가 어렴풋이 보이던 혼란 제거)
- 구간 벽 구조 차별화 (충돌 판정 불변): 원룸(전봇대·전선·실외기·빌라 철문) /
  상가(닫힌 셔터 2곳·어닝·보도 단차 — 닫힌 셔터가 많을수록 세탁소 반열림이 도드라진다) /
  놀이터(담 너머 수목 실루엣·펜스 연장·레일) / 정류장(횡단보도·정지선·볼라드·연석) /
  먹자골목(현수막·어닝·돌출 간판·쌓인 상자)
- 시식 연출 강화 (Peak-End의 End): 귀가 비트("불도 안 켜고 봉투를 뜯는다") →
  결과별 배경 틴트(바삭=웜)에서 한 입씩 — 입마다 크런치, 먹는 동안에도 식는다 →
  밤별 마무리 모놀로그 (story.md 각 밤의 '마지막 컷')
- 밸런싱 헤드리스 실측(전 왕복 자동 플레이): 걷기 61% 미지근 ✓ / 질주 77% 바삭 ✓ /
  걷기+과잉 우회 2회 34% → 목표(눅눅) 미달이라 샛길 온도 비용 9→12로 조정 → 25% 눅눅 ✓
- 헤드리스 플레이테스트 훅 `window.__fries.state()` (상태 읽기 전용)

## v0.3.1 — 2026-07-29 (프롤로그)

- 프롤로그 구현 (story.md §2): 이불 속 → 배달 앱(최소주문 12,000원의 절망) →
  화면을 끄고, 못 참고 다시 켜지고, 다시 끄고 → "감자튀김 하나에 만육천칠백 원은
  좀 아니지." → 이불을 걷으면 타이틀
- 첫 시작에만 재생 — 이어하기(진행 기록 있음)면 타이틀로 직행
- 타이틀 카드를 story.md 사양으로: 부제 "걸어서 15분. 별일 없으면." (M0 표기 제거)
- 수정: 숨긴 오버레이의 투명 버튼이 아래 층 버튼 클릭을 가로채던 히트테스트 버그
  (.overlay.hidden의 자식까지 pointer-events 차단)

## v0.3.0 — 2026-07-29 (M1 착수 — 구간 테마 5종·이상현상 10종·데이터 분리)

- 구간 테마 5종 (story.md §4 무대): 원룸 골목(배출장·센서등·창문) / 상가 골목(세탁소·간판들) /
  놀이터 옆길(펜스·그네·공) / 정류장 앞(부스·신호등 2기) / 먹자골목 입구(간판들·입간판)
  — HUD에 구간 이름 표시 ("2/5 — 상가 골목")
- 이상현상 4종 → 10종: 센서등 점등(A-002), 세탁소 셔터 반열림(A-004), 간판 점등(A-006),
  그네 흔들림(A-007), 공 이탈(A-009), 신호등 양쪽 빨간불(A-011) 신규 + 기존 4종 구간 재배정
- 신호등 정상 상태 = 초록↔빨강 교대 점등 — "학습된 정상"이 있어야 위반이 성립
- 콘텐츠 데이터 분리: src/data/anomalies.json — 이상현상 추가에 코드 수정 불필요
  (새 시각 효과가 필요할 때만 world.ts 핸들러 추가)
- 이상 선택 로직: 구간·밤 기반 풀 필터링 (구간 테마의 사물에만 이상이 걸린다)
- 전 10종 헤드리스 스크린샷으로 정상/이상 가시성 검증 (신호등 초록/빨강, 그네 흔들림 프레임 등)
- docs/anomalies.md 전면 개편 — 구간별 현황판(✅/⬜), 추가 3단계 가이드, 실스키마 반영

## v0.2.4 — 2026-07-29 (문서 원칙 정합)

- 시작 화면의 규칙 설명 문구 제거 — 무설명 학습 원칙 (조작·이어폰 안내만 유지)
- 샛길 어포던스: 입구 틈의 한색 조명 + 본길보다 밝은 바닥 (design-principles §0 시그니파이어)
- 재활용 배출장 프롭 추가 — 우산 이상(A-001)의 '정상 상태' 학습 대상 (fear-cognition §1)
- 온도 감쇠 0.55→0.9 — 걷기=미지근·질주=바삭·걷기+우회 2회=눅눅 (design-principles §4 목표)
- 시식 결과 "온도 N%" → "바삭함 N%" — 게이지 라벨과 손실 프레이밍 일관
- 이어하기 문구 톤 정리
- 시작 화면 설정(밝기·소리)을 "⚙ 설정" 접이식 패널로 — 타이틀 게이트 첫인상 정리
  (visual-polish §1 마감 예산 1순위). 슬라이더 기능·미리보기는 그대로 유지
- 디버그 파라미터 `?a=<effect>` — 이상현상 강제 (플레이테스트·검증용).
  헤드리스 브라우저 스크린샷으로 전 요소(4종 이상현상·샛길 조명·배출장·HUD·저장 UI)
  가시성·동작 검증 완료

## v0.2.3 — 2026-07-29 (입간판 이상현상 수정)

- 입간판(A-013)이 사실상 보이지 않던 문제 수정 — 정상/이상 상태가 뒤바뀌어 있었고
  (평소에 이미 플레이어를 향해 있고 이상 시 옆으로 돌아감), 안개에 먹히는 구간 후반에
  가로등 빛도 닿지 않는 위치에 벽과 같은 명도로 놓여 있었다
- A자형 입간판 형태로 교체(실루엣 식별), 가로등 사거리 안 + 판정 지점 16m 앞으로 이동,
  판면을 밝은 색(`#8b90a8`)으로 변경
- 이상현상 배치 규칙을 docs/anomalies.md에 명문화 (광원·관찰 시간·실루엣 대비)

## v0.2.2 — 2026-07-29 (기기 내 저장)

- 진행 저장(src/save.ts): 밤 진행·실패 횟수·밤별 시식 결과·밝기 설정을 localStorage에 저장
  — 서버 전송 없음, 손상 데이터 방어(sanitize), 프라이빗 모드에서도 플레이 가능
- 시작 화면 이어하기 안내("이어하기: 밤 N부터") + "기록 삭제 · 처음부터" 버튼(이중 확인)
- 개인정보처리방침 문서(docs/privacy.md) + 시작 화면 하단 안내 문구·링크
- GitHub Pages 실배포: 라이브 URL(https://ff-1204.github.io/fries-get-cold/) 문서 반영
- 배포 방식 확정: GitHub Actions 미사용, 로컬 빌드 → gh-pages 브랜치(`npm run deploy`)
- 빌드 환경: Application Control 정책이 rollup 네이티브 모듈을 차단해
  공식 WASM 빌드(@rollup/wasm-node)로 대체 (overrides)
- 밝기 조절 미리보기: 슬라이더를 잡는 동안 시작 화면이 걷혀 실제 골목을 보면서 조절
- 사운드 🔊/🔇 토글: 시작 화면 설정 줄 + M 키(게임 중, PC), 설정 기기 내 저장
  — 게임 화면에는 UI를 띄우지 않음 (몰입 우선)

## v0.2.1 — 2026-07-28 (PC·모바일웹 방어 처리)

- Android 뒤로가기/브라우저 백버튼 → 게임 이탈 대신 일시정지 오버레이 (히스토리 트랩)
- 걷는 중 새로고침·탭 닫기 실수 방지 (beforeunload 가드)
- 당겨서 새로고침·핀치 줌·길게 눌러 메뉴 차단 (overscroll/gesturestart/contextmenu)
- 플레이 중 화면 꺼짐 방지 (Wake Lock, 복귀 시 재요청)
- iOS 스와이프 이탈 후 bfcache 복귀 시 오디오·시계 복구 (pageshow)

## v0.2.0 — 2026-07-28 (문서 원칙의 코드 반영)

- 프로시저럴 사운드 엔진(src/audio.ts): 바람 환경음 상시 재생, 이상 구간에서 잦아드는
  '정적' 연출, 이동 발소리, 시식 '바삭' 크런치(바삭할수록 밝은 소리) — 에셋 없이 WebAudio 생성
- 온보딩 보장: 밤 1 첫 구간은 항상 정상, 밤 1 편도에 이상현상 최소 1회 보장
- 새벽의 깊이: 경과 시간에 따라 안개 밀도 미세 증가
- 온도 게이지 라벨 "바삭함"으로 변경 (손실 프레이밍)
- 시작 화면 밝기 슬라이더 (톤 매핑 노출 제어)
- 밤별 인트로 모놀로그 5종
- 백그라운드 전환 시 오디오 일시정지

## v0.1.0 — 2026-07-28 (M0 그레이박스 프로토타입)

- 골목 구간 프리팹 + 본길/샛길 우회 판정 시스템
- 왕복 구조: 가는 길(관찰) / 오는 길(온도 게이지, 바삭·미지근·눅눅 판정)
- 이상현상 4종 (창문 불빛, 가로등 깜빡임, 우산, 입간판)
- 새벽 경과에 따른 이상 발생 확률 상승
- PC(포인터락+WASD) / 모바일(반분할 터치) 입력 추상화
- 실패 연출(암전 → 침대), 밤 진행 루프
- GitHub Pages 배포 설정(Vite base, Actions 워크플로), 로컬 단일 파일 빌드(play-local.html)
- 수정: 오버레이 표시 중 포인터락 해제/재획득 (PC에서 버튼 클릭 불가 버그)
